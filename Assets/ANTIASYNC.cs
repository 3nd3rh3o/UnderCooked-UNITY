

using System.Collections.Generic;
using UnityEditor.SearchService;
using UnityEngine;

public class ANTIASYNC : MonoBehaviour
{
    //public List<BaseAgent> agents = new List<BaseAgent>();
    void Start()
    {
        /*foreach (BaseAgent go in Object.FindObjectsByType<BaseAgent>(FindObjectsSortMode.None))
        {
            agents.Add(go);
        }*/
    }

    void Update()
    {
        /*for(int i = 0; i < agents.Count; i++)
        {
            agents[i].myUpdate();
        }*/
    }
}